import { Layout } from "../components/layout";

export default function CrossBorder() {
  return (
    <Layout title="Cross-border flows">
      <section className="hero narrow">
        <p className="eyebrow">Cross-border</p>
        <h1>Capital moving between markets</h1>
        <p>Ranked official flow lanes will replace this fixture view. Capflies will not imply bilateral precision where the published data does not support it.</p>
      </section>
    </Layout>
  );
}
